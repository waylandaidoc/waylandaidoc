# waylandaidoc.github.io

使用 `mkdocs-terminal` 主题搭建的 MkDocs 文档站模板。

## 本地运行

```bash
pip install -r requirements.txt
mkdocs serve
```

## GitHub Pages

仓库内已包含 `.github/workflows/pages.yml`，推送到 `main` 后会自动构建并发布到 `https://waylandaidoc.github.io/`。
